# Scripts do Laboratório
Nesta pasta serão armazenados scripts de automação, comandos de instalação do Wazuh e configurações de inicialização dos serviços.
# Guia de Execução de Scripts

## 1. Servidor Wazuh (VM 2)
Para instalar o servidor centralizado do Wazuh no Ubuntu Server, execute os comandos abaixo após subir a VM:

```bash
chmod +x install_wazuh.sh
./install_wazuh.sh
